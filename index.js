employee = function createEmployeeRecord(firstName, familyName, title, payPerHour, timeInEvents, timeOutEvents) {
    this.firstName = firstName;
    this.familyName = familyName;
    this.title = title;
    this.payPerHour = payPerHour;
    this.timeInEvents = timeInEvents;
    this.timeOutEvents = timeOutEvents;
}

funciton createTimeInEvent(timeIn) {
    return employee.timeInEvents;
}

function createTimeOutEvents(timeOut) {
    return empployee.timeOutEvents;
}